spring:
  config:
    import: optional:file:../.env[.properties]
  application:
    name: station-service

  # ── Database ──
  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB:chargesquare}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: station
    open-in-view: false

  # ── Flyway ──
  flyway:
    schemas: station
    default-schema: station

# ── Server ──
server:
  port: ${SERVER_PORT:8081}

# ── Actuator ──
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

# ── Swagger / OpenAPI ──
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# ── JWT ──
jwt:
  secret: ${JWT_SECRET}

# ── Logging ──
logging:
  level:
    com.yusufakdogan.station_service: DEBUG
    org.springframework.web: INFO
    org.flywaydb: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
